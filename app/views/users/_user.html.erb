

<div class="container-fluid">
  <div class="row">
    <div class="col-md-12 col-lg-12 col-sm-12 mx-auto shadow-lg p-3 hello mb-2 mx-2">
      <div id="<%= dom_id user %>">
        <table class="table">
          <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Action</th>
          </tr>
          <tr>
            <td><%= user.id %></td>
            <td><%= user.name %></td>
            <td><%= link_to "Show this user", user, class: 'btn btn-secondary' %></td>
          </tr>
        </table>
      </div>
    </div>
  </div>
</div>